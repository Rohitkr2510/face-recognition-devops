apiVersion: v1
kind: Service
metadata:
  name: face-recognition-service
spec:
  type: LoadBalancer
  selector:
    app: face-recognition-service
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
